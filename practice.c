#include <stdio.h>
#include <conio.h>
int main()
{
    printf("Enter the num"); 
    scanf("%d",&n);

    int digit,reversed;
    while (n != 0)
    {
        digit = n % 10;
        reversed = reversed + 

    }
    
    return 0;
}
